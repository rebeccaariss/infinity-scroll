// ☝️ Set up local environment variables by copying this file to 'config.js'
// and replacing 'YOUR_UNSPLASH_API_KEY' with your actual Unsplash API key.
// This step is necessary unless the application is deployed with the API
// key configured elsewhere.

const config = {
  apiKey: 'YOUR_UNSPLASH_API_KEY',
};

export default config;
